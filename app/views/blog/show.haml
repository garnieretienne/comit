- content_for :title do
  = current_blog.name

= render 'layouts/header', title: current_blog.name, links: ''

#blog
  %ul#posts
    - @posts.each do |post|
      %li
        .asterisk
          ✱
        %a.post-description{href: path_to_blog_post(post)}
          .title= post.title
          .date= post.date.strftime("%Y-%m-%d")